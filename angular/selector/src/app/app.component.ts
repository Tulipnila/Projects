import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  /*fruits: string[] = ['Apple', 'Orange', 'Grapes', 'Plum'];
  isCheckedFruits: { [key: string]: boolean } = {};
  selectedFruits: string[] = [];

  itemClick(fruit: string) {
    this.isCheckedFruits[fruit] = !this.isCheckedFruits[fruit];
    if (this.isCheckedFruits[fruit]) {
      this.selectedFruits.push(fruit);
    } else {
      const index = this.selectedFruits.indexOf(fruit);
      if (index !== -1) {
        this.selectedFruits.splice(index, 1);
      }
    }
  }*/
 
}
